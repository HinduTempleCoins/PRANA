{
  "name": "prana-contracts",
  "version": "0.0.1",
  "description": "PRANA EVM contracts (generic DeFi primitives). Chain-agnostic; tested locally.",
  "license": "MIT",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network prana_local"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2"
  }
}
