{
  "name": "akasha-wallet",
  "private": true,
  "type": "module",
  "description": "Client-side wallet + gateway front-end libraries (private).",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "ethers": "^6.16.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
