{
  "name": "akasha-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Akasha — the PRANA wallet + explorer demo (Vite + React). Private, local-only.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "ethers": "^6.16.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16"
  }
}
